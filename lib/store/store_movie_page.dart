import 'package:flutter/material.dart';

class StoreMoviePage extends StatelessWidget {
  const StoreMoviePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("3"),
      ),
    );
  }
}
